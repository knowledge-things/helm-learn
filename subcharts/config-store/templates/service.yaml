apiVersion: v1
kind: Service
metadata:
  name: {{ include "conf-store.fullname" . }}
  labels:
    {{- include "conf-store.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "conf-store.selectorLabels" . | nindent 4 }}
