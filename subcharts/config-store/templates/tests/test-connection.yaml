apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "conf-store.fullname" . }}-test-connection"
  labels:
    {{- include "conf-store.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "conf-store.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
